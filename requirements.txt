# Core Framework (Python 3.13 compatible with pre-built wheels)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-dotenv==1.0.0
pydantic>=2.10.0
python-multipart==0.0.6

# API Clients
openai>=2.8.0
httpx==0.25.1
exa-py==2.0.1

# Data Processing (Python 3.13 compatible)
pandas>=2.2.0
numpy>=1.26.0

# Search & ML (CPU-optimized)
faiss-cpu>=1.8.0
rank-bm25==0.2.2

# PyTorch CPU-only (Python 3.13 compatible)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0+cpu
torchvision==0.21.0+cpu

# Transformers (depends on torch)
sentence-transformers>=5.1.0
